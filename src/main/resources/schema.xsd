<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema">
	<xs:complexType name="exampleType">
		<xs:annotation>
			<!-- This element causes an error when using maven-jaxb2-plugin >= 0.12.2 with jaxb-xew-plugin -->
		</xs:annotation>
	</xs:complexType>
</xs:schema>
